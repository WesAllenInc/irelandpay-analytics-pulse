'use client';

/**
 * Empty StagewiseToolbarWrapper component
 * 
 * This is a placeholder component that replaces the Stagewise toolbar
 * which was removed from the project dependencies.
 */
export function StagewiseToolbarWrapper() {
  // This component doesn't render anything visible
  return null;
}
