{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/api/functions/:path*",
      "destination": "https://*.functions.supabase.co/:path*"
    }
  ]
}
