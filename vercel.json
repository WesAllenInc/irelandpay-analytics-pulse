{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_SUPABASE_URL": "${NEXT_PUBLIC_SUPABASE_URL}",
    "NEXT_PUBLIC_SUPABASE_ANON_KEY": "${NEXT_PUBLIC_SUPABASE_ANON_KEY}",
    "SUPABASE_SERVICE_ROLE_KEY": "${SUPABASE_SERVICE_ROLE_KEY}",
    "NEXT_PUBLIC_APP_URL": "${NEXT_PUBLIC_APP_URL}",
    "NEXT_PUBLIC_SUPABASE_PROJECT_ID": "${NEXT_PUBLIC_SUPABASE_PROJECT_ID}",
    "CSRF_SECRET": "${CSRF_SECRET}",
    "IRIS_CRM_API_KEY": "${IRIS_CRM_API_KEY}",
    "SMTP_HOST": "${SMTP_HOST}",
    "SMTP_PORT": "${SMTP_PORT}",
    "SMTP_SECURE": "${SMTP_SECURE}",
    "SMTP_USER": "${SMTP_USER}",
    "SMTP_PASSWORD": "${SMTP_PASSWORD}",
    "SMTP_FROM": "${SMTP_FROM}",
    "IRIS_MAX_RETRIES": "${IRIS_MAX_RETRIES}",
    "IRIS_BACKOFF_BASE_MS": "${IRIS_BACKOFF_BASE_MS}",
    "IRIS_TIMEOUT_MS": "${IRIS_TIMEOUT_MS}",
    "IRIS_CIRCUIT_MAX_FAILURES": "${IRIS_CIRCUIT_MAX_FAILURES}",
    "IRIS_CIRCUIT_RESET_SECONDS": "${IRIS_CIRCUIT_RESET_SECONDS}",
    "TEST_USER_EMAIL": "${TEST_USER_EMAIL}",
    "TEST_USER_PASSWORD": "${TEST_USER_PASSWORD}",
    "TEST_ADMIN_EMAIL": "${TEST_ADMIN_EMAIL}",
    "TEST_ADMIN_PASSWORD": "${TEST_ADMIN_PASSWORD}",
    "PLAYWRIGHT_TEST_BASE_URL": "${PLAYWRIGHT_TEST_BASE_URL}",
    "SUPABASE_JWT_SECRET": "${SUPABASE_JWT_SECRET}",
    "IRIS_CRM_BASE_URL": "${IRIS_CRM_BASE_URL}"
  }
}
